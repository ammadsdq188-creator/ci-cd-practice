name: Parallel test workflow

on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - name: Job 1 step
        run: echo "Job 1 running"

  job2:
    needs: job1
    runs-on: ubuntu-latest
    steps:
      - name: Job 2 step
        run: echo "Job 2 running"

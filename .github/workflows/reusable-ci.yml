name: Reusable CI

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  reusable-test:
    runs-on: ubuntu-latest

    steps:
      - name: Run reusable test
        run: echo "Running CI for ${{ inputs.environment }}"
